<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= channel.name %>
    </title>
    <link href="/public/styles/style.css" rel="stylesheet">
    <script defer type="text/javascript" src="../public/scripts/script.js"></script>
</head>

<body>
    <h1>
        <%= channel.name %>
    </h1>
    <h2>
        <%= channel.description %>
    </h2>
    <h3>Messages:</h3>
    <ul>
        <% channel.posts.forEach( post=> { %>
            <li>
                <div id="header-font">
                    <%= post.by %>
                        <div id="date-time">
                            <%= post.date %>
                        </div>
                </div>
                <%= post.content %>

            </li>
            <% }) %>
    </ul>

    <ul id="messages"></ul>

    <form id="form" method="POST" action="/channel/<%= channel._id %>">
        <input type="hidden" value="<%= user.name %>" name="by" id="by">
        <input id="input" name="content" autocomplete="off">
        <button class="button">Send</button>
    </form>
</body>

</html>